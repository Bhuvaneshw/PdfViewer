# Module compose

This module provides a Jetpack Compose wrapper around the `PdfViewer` from the `:core` module. It allows you to display PDF documents in your Compose UI with a simple `PdfViewer` composable.
